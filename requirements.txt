# FastAPI e dependências
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# SQLAlchemy 2.0 e Alembic
sqlalchemy[asyncio]==2.0.25
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiosqlite==0.19.0

# Pydantic v2
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Autenticação e Segurança
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Utilitários
python-dateutil==2.8.2
pytz==2023.3

# XML e Fiscal
lxml==5.1.0
xmltodict==0.13.0

# Testes
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.26.0
faker==22.0.0

# Relatórios e Excel
openpyxl==3.1.2
reportlab==4.0.8

# Redis para cache (opcional)
redis==5.0.1

# Validação CPF/CNPJ
validate-docbr==1.10.0

# Logging estruturado e Monitoramento
python-json-logger==2.0.7
psutil==5.9.8
sentry-sdk==1.40.0  # Opcional para monitoramento de erros

# Rate Limiting
slowapi==0.1.9

# Pagamentos (PIX, Boleto, Conciliação)
qrcode[pil]==7.4.2  # QR Code PIX
pillow==10.1.0  # Manipulação de imagens
# python-boleto==0.3.5  # Geração de boletos (descomentar quando necessário)
# mercadopago==2.2.0  # Gateway PIX Mercado Pago (descomentar quando necessário)
# pyofx==0.3.0  # Parser OFX para conciliação (descomentar quando necessário)

# Certificado Digital e NF-e
cryptography==42.0.0  # Certificado digital A1/A3
signxml==3.2.2  # Assinatura XML
# python-pkcs11==0.7.0  # Certificado A3 (descomentar quando necessário)
# python-nfe==0.8.0  # Comunicação com SEFAZ (descomentar quando necessário)
# brazilfiscalreport==0.1.0  # DANFE e DANFCe (descomentar quando necessário)
